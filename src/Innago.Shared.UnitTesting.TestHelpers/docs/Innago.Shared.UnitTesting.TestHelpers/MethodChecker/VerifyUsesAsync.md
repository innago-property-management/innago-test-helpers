# MethodChecker.VerifyUsesAsync&lt;T&gt; method (1 of 4)

Verifies that the *act* uses the method indicated in *expression* on the *mock* object.

```csharp
public static Task VerifyUsesAsync<T>(Func<Task> act, Mock<T> mock, 
    Expression<Action<T>> expression)
    where T : class
```

| parameter | description |
| --- | --- |
| T | The type of the mocked object. |
| act | The Action. |
| mock | The Mock. |
| expression | The Expression of Action. |

## Remarks

This is just syntactic sugar.

## Examples

```csharp
var doSomething = Mock.Of<IDoSomething>();
 var d = new Dummy(doSomething);
 await VerifyUsesAsync(
     async () => await d.UseDoSomethingAsync2(),
     Mock.Get(doSomething),
     something => something.DoSomething());
```

## See Also

* class [MethodChecker](../MethodChecker.md)
* namespace [Innago.Shared.UnitTesting.TestHelpers](../../Innago.Shared.UnitTesting.TestHelpers.md)

---

# MethodChecker.VerifyUsesAsync&lt;T&gt; method (2 of 4)

Verifies that the *act* uses the method indicated in *expression* on the *mock* object.

```csharp
public static Task VerifyUsesAsync<T>(Func<Task> act, Mock<T> mock, 
    Expression<Action<T>> expression, Times times)
    where T : class
```

| parameter | description |
| --- | --- |
| T | The type of the mocked object. |
| act | The Action. |
| mock | The Mock. |
| expression | The Expression of Action. |
| times | The Times. |

## Remarks

This is just syntactic sugar.

## Examples

```csharp
var doSomething = Mock.Of<IDoSomething>();
 var d = new Dummy(doSomething);
 await VerifyUsesAsync(
     async () => await d.UseDoSomethingAsync2(),
     Mock.Get(doSomething),
     something => something.DoSomething(),
     Times.Once());
```

## See Also

* class [MethodChecker](../MethodChecker.md)
* namespace [Innago.Shared.UnitTesting.TestHelpers](../../Innago.Shared.UnitTesting.TestHelpers.md)

---

# MethodChecker.VerifyUsesAsync&lt;T,TResult&gt; method (3 of 4)

Verifies that the *act* uses the method indicated in *expression* on the *mock* object.

```csharp
public static Task VerifyUsesAsync<T, TResult>(Func<Task> act, Mock<T> mock, 
    Expression<Func<T, TResult>> expression)
    where T : class
```

| parameter | description |
| --- | --- |
| T | The type of the mocked object. |
| TResult | The expression result type. |
| act | The Action. |
| mock | The Mock. |
| expression | The Expression of Action. |

## Remarks

This is just syntactic sugar.

## Examples

```csharp
var doSomething = Mock.Of<IDoSomething>();
 var d = new Dummy(doSomething);
 await VerifyUsesAsync(
     async () => await d.UseDoSomethingAsync(),
     Mock.Get(doSomething),
     something => something.DoSomethingAsync());
```

## See Also

* class [MethodChecker](../MethodChecker.md)
* namespace [Innago.Shared.UnitTesting.TestHelpers](../../Innago.Shared.UnitTesting.TestHelpers.md)

---

# MethodChecker.VerifyUsesAsync&lt;T,TResult&gt; method (4 of 4)

Verifies that the *act* uses the method indicated in *expression* on the *mock* object.

```csharp
public static Task VerifyUsesAsync<T, TResult>(Func<Task> act, Mock<T> mock, 
    Expression<Func<T, TResult>> expression, Times times)
    where T : class
```

| parameter | description |
| --- | --- |
| T | The type of the mocked object. |
| TResult | The expression result type. |
| act | The Action. |
| mock | The Mock. |
| expression | The Expression of Action. |
| times | The Times. |

## Remarks

This is just syntactic sugar.

## Examples

```csharp
var doSomething = Mock.Of<IDoSomething>();
 var d = new Dummy(doSomething);
 await VerifyUsesAsync(
     async () => await d.UseDoSomethingAsync(),
     Mock.Get(doSomething),
     something => something.DoSomethingAsync(),
     Times.Once());
```

## See Also

* class [MethodChecker](../MethodChecker.md)
* namespace [Innago.Shared.UnitTesting.TestHelpers](../../Innago.Shared.UnitTesting.TestHelpers.md)

<!-- DO NOT EDIT: generated by xmldocmd for Innago.Shared.UnitTesting.TestHelpers.dll -->
